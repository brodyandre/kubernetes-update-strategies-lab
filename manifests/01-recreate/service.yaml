---
apiVersion: v1
kind: Service
metadata:
  name: recreate-service
  namespace: update-strategies
  labels:
    app: recreate-demo
    strategy: recreate
spec:
  type: ClusterIP
  selector:
    app: recreate-demo
  ports:
    - name: http
      port: 80
      targetPort: http
